# LendSQR

## Setup

Run `npm install`

Run the application

For setup ease, `docker-compose up -d`

`yarn dev`

## Knex Commands
For Knex commands, prefix all commands with `yarn knex`. For example

`yarn knex migrate:make {table-name}`

